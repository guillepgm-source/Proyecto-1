export type CutSegment = {
  trimBefore: number;
  trimAfter: number;
  startFrame: number;
  durationFrames: number;
};

export type SourceVideoDimensions = {
  sourceWidth: number;
  sourceHeight: number;
};
